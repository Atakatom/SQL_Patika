# Ödev1

### 1. Soru

> ```SQL
> SELECT title,description FROM film
> ```

### 1. Soru

> ```SQL
> SELECT title,description FROM film
> ```

### 1. Soru

> ```SQL
> SELECT title,description FROM film
> ```

### 1. Soru

> ```SQL
> SELECT title,description FROM film
> ```

### 1. Soru

> ```SQL
> SELECT title,description FROM film
> ```

### 1. Soru

> ```SQL
> SELECT title,description FROM film
> ```

### 1. Soru

> ```SQL
> SELECT title,description FROM film
> ```
